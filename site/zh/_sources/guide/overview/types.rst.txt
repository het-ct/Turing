.. Generated from platform/docs/pages/gui\types.js

het_widgets 公共类型定义
========================

.. container:: module-summary widget-summary

   **UI 组件**

   include/het_widgets.h

include/het_widgets.h

het_event_t — 事件结构体
-------------------------

回调签名：event_cb 接收只读事件对象指针，业务侧通常不应修改其内部字段。

常用宏参数：evt 均为 const het_event_t \*；HET_EVT_GET_POS 适用于坐标类事件，HET_EVT_GET_CODE 用于提取事件码，HET_EVT_GET_USER_DATA 用于取回注册时的上下文。

het_event_code — 事件类型
--------------------------

rect_t / het_pad_t
------------------

het_widget_type
---------------

参数与选项
----------

.. list-table::
   :header-rows: 1

   * - 字段
     - 类型
     - 说明
   * - original
     - void\*
     - 事件源组件句柄，回调中通常用它再调用对应组件的 getter。
   * - user_data
     - void\*
     - 注册回调时透传的用户上下文。
   * - param
     - const void\*
     - 事件附带参数，例如触点坐标、业务透传指针等，具体含义由事件源决定。
   * - flags
     - uint32_t
     - 事件类型与事件码打包字段；低 2 bit 为 type，其余高位为 code。

参数与选项
----------

.. list-table::
   :header-rows: 1

   * - 枚举值
     - 值
     - 触发时机
   * - HET_EVENT_NONE
     - 0
     - 无事件
   * - HET_EVENT_CLICKED
     - 1
     - 单击抬起
   * - HET_EVENT_LONG_CLICKED
     - 2
     - 长按抬起
   * - HET_EVENT_DOUBLE_CLICKED
     - 3
     - 双击
   * - HET_EVENT_VALUE_CHANGED
     - 4
     - 值变化（slider/switch/roller/textarea 等）

参数与选项
----------

.. list-table::
   :header-rows: 1

   * - 类型/宏
     - 参数/字段
     - 说明
   * - rect_t
     - x, y, wid, hei
     - 组件几何信息；大多数组件约定 x/y 为中心坐标。
   * - het_pad_t
     - top, left, right, bottom
     - 内边距定义，常用于文本对齐、指示器缩进等。
   * - het_point_t
     - x, y
     - 二维点坐标，常作为触摸事件参数使用。
   * - HET_GEOMETRY
     - x, y, w, h
     - 便捷构造 rect_t；分别表示位置和宽高。

参数与选项
----------

.. list-table::
   :header-rows: 1

   * - 枚举值
     - 值
   * - HET_WIDGET_TYPE_LABEL
     - 100
   * - HET_WIDGET_TYPE_BUTTON
     - 101
   * - HET_WIDGET_TYPE_IMAGE
     - 102
   * - HET_WIDGET_TYPE_BAR
     - 103
   * - HET_WIDGET_TYPE_ANIM
     - 104
   * - HET_WIDGET_TYPE_SLIDER
     - 105
   * - HET_WIDGET_TYPE_SWITCH
     - 106
   * - HET_WIDGET_TYPE_TEXTAREA
     - 107
   * - HET_WIDGET_TYPE_ROLLER
     - 108
   * - HET_WIDGET_TYPE_LED
     - 109

使用示例
--------

.. code-block:: c

   typedef struct {
       void       *original;
       void       *user_data;
       const void *param;
       uint32_t   flags;      // bit0-1: type, bit2-31: code
   } het_event_t;
   
   typedef void (*event_cb)(const het_event_t * const e);
   
   // 便捷宏
   #define HET_EVT_GET_POS(evt)        (const het_point_t *) evt->param
   #define HET_EVT_GET_TYPE(evt)       (het_event_type) (evt->flags & 0x03)
   #define HET_EVT_GET_CODE(evt)       (het_event_code) (evt->flags >> 2)
   #define HET_EVT_GET_USER_DATA(evt)  (evt->user_data)

.. code-block:: c

   typedef struct { s16 x, y, wid, hei; } rect_t;
   typedef struct { int8_t top, left, right, bottom; } het_pad_t;
   typedef struct { int16_t x, y; } het_point_t;
   
   // 快捷宏
   #define HET_GEOMETRY(x, y, w, h)  (const rect_t){x, y, w, h}
